@model IEnumerable<Biosite.Domain.Substance.Entities.Nutraceutical>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section styles{
    <link rel="stylesheet" href="~/plugins/morris/morris.css">
    <link href="~/Content/style.css" rel="stylesheet" />
}

<p>
    <h2>@Html.ActionLink("Incluir Nutracêutico", "Create")</h2>
</p>
<table class="table">
    <tr>
        <th>
            Nome
        </th>
        <th>
            Tipo
        </th>
        <th>
            Status
        </th>
        <th>
            Ação
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NutraceuticalDescriptionType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasOk)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id = item.Id })
        </td>
    </tr>
}

</table>
